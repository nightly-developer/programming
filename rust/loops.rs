
fn main() {

}
//---------------------loops-------------------
    /*{    
    let arr:[i32;3] = [1,2,3];

    square(arr);
    
    //value of arr are not modified
    println!("for using while");
    let mut count = 0;
    while count < arr.len() {
        println!("{}",arr[count]);
        count += 1;
    }
}
fn square(mut array:[i32;3]) {
    //for using iter() method
    println!("for using iter()");
    for number in array.iter(){
        println!("{}",number*number);
    }
    //for using range and mutating its value
    println!("for using range");
    for i in 0..array.len(){
        array[i] = array[i]*array[i];
        println!("{}",array[i]);
    }
}*/
//-----------------fibonacci sequence----------------
/*{
    let num = 5;
    let mut a = 0;
    let mut b = 1;
    let mut fib: i32;
    println!("{}",a);
    println!("{}",b);
    for _ in 0..num {
        fib = a + b;
        a = b;
        b = fib;
        println!("{}",fib);
    }
    //println!("{}", fib);
}*/
